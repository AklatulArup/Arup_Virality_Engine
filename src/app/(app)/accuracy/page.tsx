import { AccuracyScreen } from "@/components/accuracy/accuracy-screen";

// Accuracy — per-platform backtest accuracy + the improvement from calibration,
// in plain English. Companion to the Trust Center's live-grade view.

export default function AccuracyPage() {
  return <AccuracyScreen />;
}
