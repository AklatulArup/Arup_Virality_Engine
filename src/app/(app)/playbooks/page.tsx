import { PlaybooksScreen } from "@/components/playbooks/playbooks-screen";

// Playbooks — what breaks out in the niche, mined from the evidence pool.

export default function PlaybooksPage() {
  return <PlaybooksScreen />;
}
